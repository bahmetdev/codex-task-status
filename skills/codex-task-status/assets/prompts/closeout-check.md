First distinguish initialization from a later update: on a new task's first substantive turn, including analysis or consultation, read the live title and initialize the approved subject format plus truthful state exactly once. An automatic app-generated title is not a user naming choice merely because it exists; preserve explicit names or evidenced UI renames. After verified initialization, only the status changes automatically. Later prompts, resume, restart, and compaction do not restart naming. Preserve established titles with uncertain history; skip any write if the live title is unavailable.

Before the final response, classify the task's current verified state exactly once. If the suffix is stale, update only that suffix and verify the result. Preserve the stable subject. If title controls are unavailable or fail, report the stale title rather than claiming synchronization. Do not let this check block otherwise truthful completion. Before any title write, reread the live title and retain the exact user-chosen subject, including multiple words or another language. A user rename supersedes the initial name; never restore a cached subject. Replace only a recognized trailing workflow suffix and preserve other parentheses; if a fresh title is unavailable, skip the write.
