Before the final response, classify the actual outcome using the approved naming policy. Required agent work and real waiting gates take precedence over Git milestones; do not invent a QA or approval step. Routine recurring runs retain their verified schedule.

Read the live title. If its recognized trailing workflow suffix is stale, replace only that suffix, preserving the exact current subject and other parentheses. Do not reinitialize an established task or restore a cached name. Skip the write if the live title is unavailable. Verify any update; report an unavailable or failed title update without blocking otherwise truthful completion.

If no workflow suffix is recognized, preserve the entire live title as the subject and append the approved truthful suffix.
